# Backend (masterfabric-go) base URL the app calls.
# On a physical device/emulator "localhost" is the device itself — use your
# machine's LAN IP (e.g. http://192.168.1.20:8081) for local dev, or your
# Render URL in production.
EXPO_PUBLIC_API_URL=http://localhost:8081
